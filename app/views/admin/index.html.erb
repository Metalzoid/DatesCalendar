<h1>COUCOU <%= current_admin.email %></h1>

<%= link_to "Destroy", destroy_admin_session_path, data: {
                    turbo_method: :delete,
                    turbo_confirm: "Are you sure?"
                  } %>
